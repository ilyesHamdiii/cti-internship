# Testing

Automated tests use deterministic AI fixtures by default. Live DeepSeek smoke tests are environment-gated.

Commands:

```bash
make backend-test
make backend-lint
make compose-config
make demo
```
